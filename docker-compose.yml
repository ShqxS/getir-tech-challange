version: "3"
services:
  book-service:
    build: book-service/.
    networks:
      - getir-network
    ports:
      - "9090:9090"
  order-service:
    build: order-service/.
    networks:
      - getir-network
    ports:
      - "9091:9091"
  auth-service:
    build: auth-service/.
    networks:
      - getir-network
    depends_on:
      - mongo
    ports:
      - "9092:9092"
  mongo:
    image: mongo
    ports:
      - "27017:27017"

networks:
  getir-network: